[project]
name = "core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "kafka-python>=2.3.0",
    "msal>=1.34.0",
    "pre-commit>=4.5.1",
]

[project.scripts]
core = "core.main:main"

